name = "weopqrst/mooncassette-async"

version = "0.1.0"

license = "Apache-2.0"

preferred_target = "js"

description = "Async adapters for mooncassette: use it with async HTTP clients (and therefore over a real network) while the core library stays dependency-free."

import {
  "moonbitlang/async@0.21.3",
  "weopqrst/mooncassette@0.4.0",
}
