// mooncassette/canon —— JSON 规范文本与跨目标稳定哈希。
//
// 这是整个项目的地基之一：指纹的可复现性、cassette 可稳定 diff，
// 都建立在「同一语义的 JSON 一定产出同一段文本」这一性质上。
// 因此本包刻意不依赖标准库的 Map 迭代顺序与 Hasher 实现。
import {
  "moonbitlang/core/double",
  "moonbitlang/core/encoding/utf8",
}

import {
  "moonbitlang/core/json",
} for "test"
